'use strict';

function MyProject('name', 'date', 'url') {
  this.name = name;
  this.date = date;
  this.url = url;

}

var codeFellowsProject = new MyProject('Salmon Cookies', 'date', 'url');

// to be filled later
